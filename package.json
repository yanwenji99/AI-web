{
  "name": "ai-web-local-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Local web AI proxy via browser automation",
  "type": "module",
  "scripts": {
    "dev": "tsx apps/server/src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "playwright": "^1.53.0"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
